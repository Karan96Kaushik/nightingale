version: 1
backend:
  phases:
    build:
      commands:
        - npm i --cache .npm --prefer-offline
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - export VITE_SUPABASE_URL=https://rygjdodnzfwynizmywxc.supabase.co
        - export
          VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_IGEtlpU0z1XYagyCmIJWtg_ShKaLJAG
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
